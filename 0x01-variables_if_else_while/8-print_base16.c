#include <stdio.h>
/**
 * main - Entry
 * Description: 'Print numbers of base 16'
 * Return: always 0
 */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++);
	{
		printf("%d", n);
	}

	for (m = 97; m <= 102; m++);
	{
		printf("%d", m);
	}
	printf("\n");
	return (0);
}
